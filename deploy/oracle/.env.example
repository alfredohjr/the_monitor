# Copie para deploy/oracle/.env (NÃO versionado) e preencha com valores reais.

# Postgres
POSTGRES_USER=appuser
POSTGRES_PASSWORD=troque-por-uma-senha-forte
POSTGRES_DB=appdb

# Domínio servido pelo Caddy (Variante A)
DOMAIN=seudominio.com

# Versão carimbada nas imagens buildadas na VM (/version e rodapé do front)
APP_VERSION=0.4.0-dev

# Variante B (Cloudflare Tunnel) — token do túnel do Zero Trust
# TUNNEL_TOKEN=coloque-o-token-aqui
